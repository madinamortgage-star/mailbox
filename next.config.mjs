/** @type {import('next').NextConfig} */
const nextConfig = {
  // YouTube iframes are plain HTML — no Next.js config needed.
  // next/font/google self-hosts Inter, so no external font domains required.
};

export default nextConfig;
